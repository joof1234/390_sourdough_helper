<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@color/background_peach">

    <TextView
        android:id="@+id/bt_guide_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="center_vertical"
        android:text="Only paired devices to your phone will Show up on the list to select."
        android:textColor="@color/charcoal"
        android:textSize="20dp" />

    <!-- Scan for Devices Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnScan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:backgroundTint="@color/light_salmon"
        android:text="Scan for Devices"
        android:textColor="@color/charcoal"
        app:cornerRadius="16dp" />

    <!-- CardView for Devices List -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/device_card"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_margin="10dp"
        android:layout_weight="1"
        android:backgroundTint="@android:color/white"
        app:cardCornerRadius="16dp"
        app:cardElevation="4dp">

        <ListView
            android:id="@+id/lvDevices"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@color/brand_accent"
            android:dividerHeight="1dp"
            android:padding="12dp"
            android:clipToPadding="false" />
    </com.google.android.material.card.MaterialCardView>

    <!-- Horizontal Section for Send Button and Status -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_margin="10dp">

<!--        <com.google.android.material.button.MaterialButton-->
<!--            android:id="@+id/btnSend"-->
<!--            android:layout_width="0dp"-->
<!--            android:layout_height="wrap_content"-->
<!--            android:layout_weight="1"-->
<!--            android:layout_marginEnd="10dp"-->
<!--            android:enabled="false"-->
<!--            android:text="Send Test Message"-->
<!--            android:textColor="@color/charcoal"-->
<!--            android:backgroundTint="@color/soft_mint"-->
<!--            app:cornerRadius="16dp" />-->

        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:paddingStart="16dp"
            android:textSize="30dp"
            android:text=""
            android:textColor="@color/charcoal" />
    </LinearLayout>

    <!-- Complete / Next Step Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/complete_bluetooth"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="Next Step"
        android:textColor="@color/charcoal"
        android:backgroundTint="@color/pale_lavender"
        app:cornerRadius="16dp" />

</LinearLayout>
